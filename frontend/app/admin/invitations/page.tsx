'use client'

import InvitationsClient from './InvitationsClient'

/** Admin Panel → Invitations. Chrome and the auth gate live in the layout. */
export default function AdminPanelInvitationsPage() {
  return <InvitationsClient />
}
