"""Backlink Intelligence (roadmap M2 / implementation Phase 8).

Backend only in this pass — the inventory UI (P8.3) and the licensed vendor
adapter (P8.2, blocked on operator item A4) are deliberately absent. Everything
here runs end-to-end at $0 against ``MockBacklinkSource``.
"""
