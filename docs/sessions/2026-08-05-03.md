# Session 22 — 2026-08-05: the Admin Panel becomes a product surface

Operator prompt at start: *"finish admin panel and backlink methodology using
workflows"*, followed by a seven-part brief: rename the administration
interface to **Admin Panel** everywhere; build user invitations; complete role
management; build a queryable, tamper-resistant database audit trail; generate a
roadmap-aligned backlog; validate with workflows; extend GitHub Actions to gate
all of it; and a quality bar (no failing tests, no type errors, no broken
routes, APIs documented, docs current).

## 0. What was already true, measured before touching anything

`uv run pytest` on the merged branch: **752 passed, 13 skipped**. Frontend:
**232 across 46 files**. Both green.

The single most important finding of the start ritual: **the plan documents
were a session stale, and in the direction that matters.** `implementation-plan.md`
listed P7.2 (RBAC) and P7.3 (audit spine) as `todo`; both had actually shipped
in session 21, along with `admin_routes.py`, `permissions.py`, `audit.py`,
migrations 0012–0017, and a frontend `/admin` screen. `admin-panel-plan.md`
still opened with "no code exists yet". Building from the documents would have
duplicated a milestone's worth of work.

So the first substantive act was to read the code rather than the plan, and the
last was to make the plan match the code. Both statuses are corrected in this
session's diff.

## 1. What the brief asked for, against what existed

| Asked for | State at session start |
|---|---|
| Rename to "Admin Panel" | The surface existed as *"Members & roles"*, a leaf item under Settings. |
| User invitations | **Nothing.** No table, no endpoint, no screen. `MEMBER_INVITE` was a permission nobody could exercise. |
| Assign / change roles | Done (PATCH `/members/{id}`). |
| Disable / reactivate | Done (same route). |
| **Remove** roles | **Missing.** `MEMBER_REMOVE` was defined and unused — no route deleted a membership. |
| Audit trail: who/what/before/after/when/operation | Recorded. |
| Audit trail: request id, user id, IP | Columns existed; **every one was NULL in every row.** |
| Audit trail queryable from the panel | **No API and no screen.** The service had `events_for_org` and `entity_timeline`; nothing called them. |
| Tamper resistance | Append-only by *convention* — no service issued an UPDATE, which is a claim about code, not about the database. |
| Backlog | Not written. |

## 2. What was built

### 2.1 Invitations, end to end (ADR-37)

New `invitations` table (migration 0018), `services/invitations.py`,
`api/invitation_routes.py`, and two screens. The decisions worth recording:

- **The token is never stored** — a SHA-256 is, exactly as `auth_sessions`
  treats refresh tokens. Plain SHA-256 rather than a password hash because the
  value carries 256 bits of CSPRNG entropy: there is nothing for a slow hash to
  defend, and an Argon2 verify on a public unauthenticated endpoint is a
  denial-of-service surface.
- **The failures are specific**, which is the opposite of what this codebase
  does everywhere else. A cross-org member id answers 404 precisely so it
  cannot be used to enumerate accounts; an invitation answers *expired* /
  *withdrawn* / *already used* / *not valid* distinctly, because a 256-bit
  token is not enumerable and telling somebody holding a real link "this
  expired on Tuesday" is the difference between a recoverable moment and a dead
  end.
- **Expiry is derived, never stored.** A status column would need a sweeper and
  would be wrong between runs.
- **The invited address is authoritative** — there is no field to override it,
  or an invitation to alice@ would be a way to create an account as bob@ with
  alice's granted role.
- **An existing account must sign in before accepting.** Accepting anonymously
  with a *new* password would be an account takeover by anyone who intercepted
  the link.
- **Email honesty.** `emails_enabled` defaults False in every environment, so
  the create response reports `email_sent: false` and hands back the link
  rather than claiming a send that never happened. The Admin Panel shows it,
  once.

### 2.2 The audit trail became usable as evidence (ADR-38, ADR-39)

Three separate gaps, each of which made the trail weaker than it looked:

1. **Request identity was never captured.** `RequestContextMiddleware` now sets
   a `ContextVar` that `audit.emit` reads as a default, so every pre-existing
   call site gained request id, hashed IP and user agent without being
   rewritten. `ContextVar` and not a thread-local, because Starlette may resume
   a coroutine on a different worker thread; reset in a `finally`, without
   which a pooled task attributes the next request to the previous caller's IP.
2. **Auth events belonged to no organization.** `audit_events` is queried *by
   org*, so a login written with `org_id = NULL` was a sign-in trail nobody
   could read. Signup and login are now attributed — including a failed login
   for a *known* address, since "somebody is guessing at my colleague's
   password" is exactly the event an administrator needs to find. An unknown
   address stays NULL, because it genuinely belongs to no tenant.
3. **Tamper resistance was a convention.** Now: a per-row SHA-256 of the row's
   own content, plus a Postgres trigger that raises on UPDATE or DELETE
   (`FOR EACH ROW`, so a `DELETE FROM audit_events` with no `WHERE` fails too).

   **A hash chain was rejected on concurrency grounds, not effort.** A chain
   also detects deletion, but computing one means reading and locking the chain
   head on every write — serializing every audited request in the application
   behind one row. Deletion is prevented at the database instead of detected
   after the fact. The residual is stated in the code: a superuser who drops
   the trigger first is not detectable, and closing that needs an external
   append-only sink (M8, not promised here).

Verification reports **three** verdicts, not two: `ok`, `altered`, and
`unverifiable` for rows written before the column existed. Back-filling those
would manufacture evidence; reporting them altered would cry wolf; reporting
them intact would be a claim the data cannot support.

Then the query surface the requirement actually asked for: filter (action
prefix, actor, entity type, entity id, outcome, date range), search, sort
across five columns, pagination with a true total, and per-record history.

**One subtle bug fixed in the design rather than found later:** the sort always
ends with `id` as a tiebreaker. Events written inside one transaction share a
timestamp to microsecond precision often enough that without it page 2 repeats
a row from page 1 and skips another — an ordinary annoyance in a product list
and a serious one in a log somebody is reading to prove a negative. There is a
test that walks four pages and asserts no id appears twice.

### 2.3 Role management completed

`DELETE /api/v1/admin/members/{id}` removes the **membership**, never the user.
Two different things get confused whenever a product offers one button for
both: the person may hold seats in other organizations, and their authored rows
— analyses, audits, the audit trail itself — must survive their departure or
the history becomes unreadable. Guarded exactly like a role change (last owner,
self), audited with the role that was lost, and it asks for confirmation in the
UI because it is the only irreversible action on that screen.

### 2.4 The rename, done properly

`/admin` is now a **top-level nav section** called **Admin Panel** with three
tabs, not a leaf under Settings. Governance is a different job done by a
different person from "change my password", and burying it one level down under
a personal-preferences heading is how a governance surface becomes invisible to
the person who needs it. Breadcrumb, `<h1>`, nav label, flyout title, section
routing and the Settings cross-link all say Admin Panel.

A real bug fell out of the nav test: `flyoutItemActive` matched by subtree, so
`/admin` (Members) lit up while you were reading `/admin/audit`. Fixed with an
exact-match set that already existed for the two visibility overviews.

### 2.5 CI (ADR-40)

- A **formatting gate scoped to changed files**. Repo-wide is impossible today
  — ~47 backend files on `main` predate any format gate (tech-debt #57) — and
  the two repo-wide outcomes are both bad: red CI on every unrelated PR, or a
  reformat-the-world commit that buries every diff after it. Scoped means the
  unformatted set can only shrink. The residual sweep stays open and is now
  described accurately in #57 rather than as "not done".
- **Named gates** so a red check says which promise broke: migrations
  (up/drift/down) + the append-only trigger; authorization rules; Admin Panel
  API. A generic "pytest failed" on a governance surface is the difference
  between a five-minute fix and a bisect.
- e2e now runs **journeys** and the **viewport matrix**, not only the happy
  path — and the stack gets `PUBLIC_BASE_URL`, without which invitation links
  would be built from a default that happens to be right.

## 3. Verification actually performed

Not "the tests pass" — what was run, and against what.

| Check | Result |
|---|---|
| `uv run pytest` (default, hermetic SQLite) | **820 passed, 22 skipped** (from 752/13) — the extra skips are the three Postgres-only modules, which is exactly the vacuous-pass problem §3.5 describes |
| `uv run pytest` with `TEST_DATABASE_URL` set (the `make test` path) | **835 passed, 7 skipped** — the Postgres-gated modules run, including the six new append-only/TRUNCATE tests and the three owner-lock ones |
| `pytest tests/test_migrations.py` against real Postgres 16 | 3 passed — upgrade head clean, **zero model/migration drift**, downgrade base reverses the chain |
| `pytest tests/test_audit_append_only_postgres.py` against real Postgres | 6 passed — UPDATE, single DELETE, bulk DELETE and **TRUNCATE** all refused; a `TRUNCATE ... CASCADE` of another table cannot reach it; INSERT still works |
| `pytest tests/test_owner_guard_postgres.py` against real Postgres | 3 passed — the owner-set row lock is taken and a second transaction blocks on it |
| `npm test` | **281 passed across 51 files** (from 232/46) |
| `npx tsc --noEmit` | clean |
| `npm run lint` | clean |
| `uv run ruff check .` / `uv run mypy app` | clean |
| Playwright `journeys.spec.ts` + `viewports.spec.ts` against a live stack | **31 passed** (16 journeys + 15 viewport checks), then re-run against the fixed stack: 30 passed, **1 load flake** — see below |
| `actionlint` on all four workflows | clean |

The Postgres checks were run against a throwaway `postgres:16` container, and
the e2e run against a real API + web pair with a migrated database — not a
mock. Two e2e failures were found and fixed: an ambiguous `getByRole('alert')`
locator (Next renders a permanently-empty route announcer, so that role is
ambiguous on every page in this app), and a 15s timeout on a client-side
redirect that has to wait for `next dev` to compile the route.

**The one failure in the final re-run was diagnosed rather than retried until
green.** `viewports.spec.ts` "signed-in pages do not overflow at desktop" timed
out waiting for the post-signup redirect — while three suites were sharing the
machine. The evidence that it is load and not a defect: the server side
*succeeded*, with `auth:signup` and `auth:login` both recorded for that exact
address in the audit table 0.4s apart; and re-run alone on an idle box all six
sizes passed in 10–13s each, against a 30s budget. The assertion was measuring
machine load, so its timeout is now 60s with that reasoning written next to it.
Recorded here rather than quietly re-run, because "flaky test" is a claim that
needs the same evidence as any other.

## 3.5 What the adversarial pass found — and it found real things

Nine verifiers were each given one claim from the brief and told to **refute**
it, defaulting to refuted when they could not positively establish it. Five
claims survived; four were refuted, and every refutation was a genuine defect
that is now fixed. Recording them because "we ran a validation pass" means
nothing without saying what it caught.

**Blocker — the last-owner guard was a non-atomic check-then-act.** Count the
active owners, then demote one. Two administrators pressing the button on the
two remaining owners at the same moment each read a count of 2, each pass the
guard, and the organization is left with **no owner at all** — recoverable only
by a support ticket and a hand-written UPDATE. Two browser tabs are enough; it
needs no malice and no load. `_owner_count` now selects the owner memberships
`FOR UPDATE` rather than counting them with an aggregate, so the second
transaction blocks, re-reads, and refuses. Proved with two live Postgres
connections and a `lock_timeout` (`test_owner_guard_postgres.py`) — the SQLite
unit suite *cannot* fail if that lock is deleted, which is exactly why the test
had to live somewhere else.

**Major — a Manager could mint an Owner.** `member:invite` is a Manager
permission and `owner` was on the assignable list, so a Manager who could not
*promote* anyone to Owner could *invite* one, and then be administered by the
account they had just created. The invitation path was a way around the check
that guards promotion. Fixed with the general rule rather than a special case:
**you cannot grant what you do not hold** — a role is offerable only if its
permission set is a subset of the caller's own. One consequence looks like a
bug and is not: an Admin cannot appoint a Billing Admin, because `billing:manage`
is precisely the permission an Admin is denied.

**Major — disabling an account did not stop the account.** The membership
flipped and the login was refused, but the *existing* access token kept working
until it expired, because a JWT is self-contained and nobody can recall it. So
"disabled" meant "disabled from their next login", which is a much weaker
promise than the button implies. Fixed at both ends: `get_current_user` refuses
a non-active user on every request, and disabling revokes their refresh
sessions so they cannot mint a new token either.

**Major — the append-only trigger did not cover `TRUNCATE`.** A row-level
trigger does not fire on TRUNCATE; Postgres swaps the file rather than
iterating rows. So `TRUNCATE audit_events` would have erased the entire trail
using nothing but the application's own database role — and that is a *more*
plausible act than the per-row DELETE the first trigger blocked, because it is
one word and it is what somebody reaches for to "clear the log". A second,
statement-level trigger now refuses it. This one is the clearest argument for
running the pass at all: the first design was tested, documented, and wrong.

**Major — logout was not audited.** Session start was recorded and session end
was not, so the trail could say who arrived and never who left. Now emitted —
attributed from a user read *before* the token is spent, since afterwards there
is nothing left to attribute it to. A logout with a junk token still records
nothing, deliberately: an event there would let the audit log be used to probe
token validity.

Two nits, also fixed: the browser tab on `/admin` inherited the marketing title
rather than saying "Admin Panel", and a too-short token surfaced Pydantic's
*"String should have at least 16 characters"* verbatim to an invitee who only
knows they clicked a link.

A tenth agent then read the nine verdicts and asked what **nobody** had checked.
Its two substantive findings were both acted on. It noticed the Postgres-gated
modules pass *vacuously* on SQLite — `FOR UPDATE` is silently omitted and the
triggers do not exist there — so a silent skip in CI would leave both the
lockout and tamper-resistance guarantees unprotected while the job stayed green;
they now run as their own named CI step. And it noticed that `redeem` does not
lock the invitation row, so two simultaneous accepts of one link were
backstopped only by the unique constraint on `users.email` — safety by accident
of another table, which evaporates entirely when an *already-registered* user
accepts, because then no insert happens at all. The accept path now takes a row
lock; the preview deliberately does not, since it is unauthenticated and a lock
there would let anyone holding a token pin a row for the length of a request.

It also flagged, correctly, that at the moment it looked the fixes were still
uncommitted — which is why they are in this session's second commit rather than
described as if they had always been there.

And one finding was a test catching the codebase's own over-eagerness rather
than a bug: the audit detail key `sessions_revoked` came back as `[redacted]`,
because the redactor matches the substring **session**. That is the redactor
working as designed — it errs toward destroying a harmless field rather than
leaking a credential — so the key was renamed, not the redactor.

## 4. The backlog

Generated by a five-agent survey workflow — M1 completion, technical debt,
infrastructure, parity features, and quality/security — each grounded in files
it actually read, then deduplicated and dependency-ordered into
[backlog.md](../backlog.md). It is the prioritized queue; `roadmap.md` stays
the what/why and `implementation-plan.md` stays the card ledger.

## 5. Assumptions and residuals, recorded rather than buried

- **`audit.emit` still swallows its own errors** — right at the request level
  (a failed audit write must not turn a successful password change into a 500)
  and wrong at the completeness level. The trail is tamper-evident but not
  loss-evident. New **tech-debt #59**; the fix is an outbox and belongs to A9.
- **Workspace-scoped roles are honoured at org grain.** The role strings exist
  and `invitations.workspace_id` is written, but `workspace_grants` was never
  built, so an Editor in a three-workspace agency is an Editor in all three.
  Safe, but not the model §5 of the plan promises. New **tech-debt #60**.
- **The organization profile is read-only** — no PATCH, so a personal org keeps
  its generated name forever. `ORG_UPDATE` is already defined and granted. New
  **tech-debt #61**.
- Invitation emails go nowhere until the operator sets `EMAILS_ENABLED=1` and a
  verified `EMAIL_FROM`. The API says so honestly and the panel shows the link,
  so the feature is usable in the meantime — but the operator should know.
- **`PUBLIC_BASE_URL` must be set in production.** It is the one value the API
  cannot infer from the request that carries it, and every invitation link is
  built from it. Recorded as an operator item.
- **The frontend has no formatter contract**, and `make fmt` documents a command
  that would rewrite the whole of it into Prettier's defaults — semicolons and
  double quotes, the opposite of the code that exists. Found while building the
  CI format gate, which is why that gate deliberately skips frontend files and
  switches itself on when a config appears. New **tech-debt #62**.
- **Migration 0018's safety against the live database is reasoned, not
  rehearsed.** It is metadata-only (`ADD COLUMN` nullable, a new empty table,
  two triggers) so it takes no long lock and cannot fail on existing data, and
  nothing anywhere in the backend issues an UPDATE, DELETE or TRUNCATE against
  `audit_events` — that was checked by grep, not assumed. But it has not been
  run against a copy of production data, and prod is currently at `0017`.

## 6. Operator action required

**Yes — two items, both small**, recorded in
[operator-expected.md](../operator-expected.md):

- **B10:** set `PUBLIC_BASE_URL=https://yanki.beyondkaira.com` in the prod
  `deploy/.env` before anyone sends an invitation, or the links will point at
  `http://localhost:8140`.
- **B11 (optional, but it is what makes invitations self-serve):** set
  `EMAILS_ENABLED=1` and a Resend-verified `EMAIL_FROM`. Until then an
  administrator has to copy the link out of the panel and send it by hand,
  which works and is honestly labelled.

Everything else in this session was implementable without a human decision.

## 7. Current state — implemented, incomplete, blocked

**This branch is `feat/admin-panel-invitations-audit`, three commits, and it is
NOT merged and NOT deployed.** It was never pushed; `main` and `origin/main`
are both still `f4c33e8`, production runs image `yanki-api:f4c33e8` at alembic
`0017_user_status`, and the `invitations` table does not exist in the
production database. That is deliberate — merging to `main` auto-deploys with
no staging — but it is a decision waiting on the operator, not a closed one.

### Implemented and verified this session

| Thing | Where | Verified by |
|---|---|---|
| Admin Panel as a named top-level section, three tabs | `frontend/lib/shell-nav.ts`, `app/admin/**` | `shellNav.test.ts`, e2e tab journey |
| Invitations: mint, preview, accept, resend, revoke | `services/invitations.py`, `api/invitation_routes.py`, migration 0018 | 28 unit tests + 2 e2e journeys |
| Member removal (seat, not account) | `admin_routes.py` `DELETE /members/{id}` | 6 unit tests |
| Role ceiling — cannot grant what you do not hold | `admin_routes.assignable_by` | 3 unit tests |
| Disable stops a live session | `auth_dependencies.py`, `auth_sessions.revoke_all_sessions_for_user` | 3 unit tests |
| Last-owner lock (TOCTOU) | `admin_routes._owner_count` | `test_owner_guard_postgres.py`, real 2-connection race |
| Audit: request id, hashed IP, org attribution | `app/request_context.py`, `services/auth.py` | 4 unit tests |
| Audit: filter/search/sort/page/history/integrity | `services/audit.py`, `admin_routes.py`, `app/admin/audit/` | 27 unit tests + 13 component tests |
| Audit tamper-resistance (row hash + 2 triggers) | migration 0018 | 6 Postgres tests incl. TRUNCATE |
| Permissions on the Site Audit routes | `seo_project_routes.py` | 7 existing tests still green |
| CI gates | `.github/workflows/ci.yml`, `scripts/format_changed.sh` | actionlint + the gate run locally |

### Incomplete, and knowingly so

- **Milestone M1 stages A5–A9 are untouched**: auth completion (password reset,
  MFA, session/device UI), plans/subscriptions/quotas/credit ledger, the
  platform back office, the system pages, and the A9 hardening exit gate. §7 of
  `admin-panel-plan.md` is entirely A7/A8 and none of it exists.
- **Workspace management has no UI and no API.** Workspaces exist as a table
  and a default row; nothing lets you create or manage one.
- **The organization profile is read-only** — no PATCH (tech-debt #61).
- **Workspace-scoped roles are honoured at org grain** (tech-debt #60).
- **`audit.emit` is loss-tolerant by design** — tamper-evident, not
  loss-evident (tech-debt #59).
- **The frontend has no formatter contract**, so the CI format gate covers only
  backend files (tech-debt #62).

### Blocked, and on what

| Blocked | On |
|---|---|
| Invitation links working in production | **Operator: `PUBLIC_BASE_URL`** (item B10) |
| Invitation *emails* sending at all | **Operator: `EMAILS_ENABLED` + a verified `EMAIL_FROM`** (B11) |
| Merge, and therefore deploy | **Operator: review + merge decision** — auto-deploy means a merge is a release |
| P7.6 plans/quotas | Stripe account, and the terms text (tech-debt #50, legal) |
| P8.2 backlink vendor adapter | Vendor + budget decision (operator A4) |

### Assumptions made, recorded rather than buried

1. **The Admin Panel is org-scoped, not platform-scoped.** Everything built
   here serves an organization administering itself. The Yanki-staff back
   office is P7.7 and deliberately absent — `SUPER_ADMIN` and `SUPPORT` exist
   in the permission matrix with no surface behind them.
2. **A role ceiling is the right model for delegation** — you may grant only
   what you hold. This makes an Admin unable to appoint a Billing Admin. That
   reads as a bug and is a decision; if the operator wants Admins to delegate
   billing, the fix is to give Admin `billing:manage`, not to weaken the rule.
3. **Distinguishing invitation failures is safe** because a 256-bit token is
   not enumerable. This is the one place the codebase deliberately breaks its
   own "unknown and forbidden answer identically" rule (ADR-37).
4. **Migration 0018 is safe on the live database** — reasoned from its content
   (nullable `ADD COLUMN`, a new empty table, two triggers) and a grep proving
   nothing writes UPDATE/DELETE/TRUNCATE to `audit_events`. **Not rehearsed
   against a copy of production data.**

## 7.5 Docs inventory audit (session-rules §3 deliverable 7)

Run as a five-agent fan-out over all **58 tracked `.md` files**, each auditor
verifying claims against the code rather than against other docs.

**Verdict: the documentation set is healthy.** Most files are live-and-correct
or correctly-frozen archives. The drift that exists is concentrated and
mechanical, and traces to exactly two shipped changes that were never
propagated: PR #11's measured/simulated pipeline pivot (a pre-existing gap,
tech-debt #54) and this session's own M1/M2 landing.

| Group | Files | Spread |
|---|---|---|
| Root (README, CONTRIBUTING, SECURITY) | 3 | 1 stale, 2 live |
| `.github/` PR template | 1 | live, minor drift |
| `deploy/` (AUTODEPLOY, MIGRATION) | 2 | 1 live, 1 archive |
| `docs/` planning + reference | 24 | 8 stale, 13 live, 3 archive |
| `brandkit/` | 5 | 1 obsolete, 2 duplicate, 1 live, 1 archive |
| `docs/sessions/` | 23 | 23 archive — contiguous, no gaps or duplicate NN |

**Fixed in this session's third commit:** README's mini-map (it claimed "3
screens" while its own intro described the Admin Panel) and two Make-target
rows; `backlink-intelligence-plan.md`'s status line, which said *"no code exists
yet"* over an eleven-module package — the single most flatly-false sentence in
the set, and an invitation to rebuild a shipped subsystem; `roadmap.md`'s
"missing entirely" snapshot; a dated banner on `feature-parity.md`; the
`implementation-plan.md` readiness snapshot; `design.md`'s structure tree and
ownership map (which omitted `serp/`, `site_audit/` and `backlink/` entirely);
`agent-workflows.md`'s ownership map, same omission; `CONTRIBUTING.md` and the
PR template and `AUTODEPLOY.md` for the new `format` gate; `MIGRATION.md`'s two
references to a `deploy/INSTALL.md` that exists nowhere;
`site-audit-integration.md`'s claim that Chromium "remains isolated to the
audit-runtime image target" — there is no such target and the Dockerfile is a
single stage, so it described a boundary that does not exist;
`frontend-brandkit.md`'s broken link; `architecture-target.md`'s BacklinkSource
seam cell; and a **staleness banner on `brandkit/uploads/frontend-brandkit.md`**,
which was the retired indigo v1 palette sitting unmarked where somebody browsing
brand assets would find it first.

**Deliberately not fixed here — `docs/architecture.md` §1–2.** It is the worst
drift in the repo and it is *pre-existing*: the as-built on-call reference still
documents the four-engine panel and the `footprints/total` score that PR #11
retired three sessions ago. It is not this branch's to fix — no pipeline code
changed here — and rewriting it inside a release that auto-deploys would add
review burden to a deploy. What this session did instead was **pin it precisely**
in tech-debt #54, section by section, so it no longer needs rediscovering. §3
of that file, the Admin Panel section written this session, was audited as
accurate.

**Left to the operator**, recorded rather than decided:

1. **`docs/02-mvp.md` contradicts the live pipeline** (§5/§8 scoring, §3 step 5
   engines) — but `session-rules.md` cites it as the current *scope authority*.
   Rewriting an MVP PRD to match post-MVP code changes what that authority is.
   Either freeze it as the MVP-milestone record and repoint the scope line at
   the milestone plans, or reconcile it with a pointer to ADR-34.
2. **`brandkit/uploads/*`** — three generator-input snapshots. `02-mvp.md` is
   byte-identical to the original and currently correct; `architecture.md` is
   ~300 lines behind but self-labels as non-authoritative; the brand-kit one
   now carries a banner. Delete them or keep them as a record of what was fed
   to the generator.
3. **`docs/past-prompt.md`** has a pre-existing gap for sessions 15–19. The
   newest entry is correct, so nothing is blocked.

**Not verified:** the auditors counted test *files* (60 backend, 51 frontend
unit + 3 e2e) but did not re-run the suites — the pass counts in this log come
from my own runs, not theirs. No file was recommended for deletion.

## 8. The next session's prompt

Everything the next agent needs is the repository plus the block below.

> Start from `docs/resume-prompt.md` and follow the `docs/session-rules.md`
> start ritual — but read the **code** before the plan wherever the two could
> disagree, because session 22 opened with two Phase 7 cards marked `todo` that
> had already shipped.
>
> **First, deal with the open branch.** `feat/admin-panel-invitations-audit`
> is three commits, local-only, never pushed. It contains the Admin Panel,
> invitations, the audit trail and migration `0018`. Confirm with the operator
> whether it has been merged; if it has not, that is the first question to
> settle, because everything below assumes it lands. **Do not merge it
> yourself** — a merge to `main` auto-deploys to production with no staging.
> Two operator items gate its usefulness in prod: **B10** (`PUBLIC_BASE_URL`,
> without which every invitation link points at localhost) and **B11**
> (`EMAILS_ENABLED`, without which invitations must be copy-pasted by hand).
>
> **Then take the next work from `docs/backlog.md`**, which is the prioritized,
> dependency-ordered queue as of session 22. Its Batch 1 is ten independent P0
> items — backups, container resource caps, log caps, a real health probe, the
> preflight key check — none of which are milestone features and all of which
> are standing risks on a VPS shared with four other production tenants. The
> single cheapest and highest-value is `database-backups`: the `yanki_pgdata`
> volume is the only copy of every analysis, user and audit row, and A5–A8 each
> add another live migration on top of it.
>
> **If the operator prefers to keep advancing M1 instead**, the next cards are
> **P7.5** (auth completion — password reset repays tech-debt #49, then TOTP
> MFA and session/device management) and **P7.6** (plans, quotas, credit
> ledger), in that order; P7.6 also unblocks Phase 8's metering. Both are
> specified in `docs/admin-panel-plan.md` §4 and §3.
>
> Standing constraints, unchanged: `make test` green at every commit; docs
> change in the same session as the code; no secrets in git (the repo is public
> and CI gitleaks-scans full history); the checker go-live (P5.11) stays
> operator-gated and independent of everything above.
