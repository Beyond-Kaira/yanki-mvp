# 2026-09-18 — Top cited pages: CSV export tests

## Scope and assumptions

User requested step-by-step work with explanations and approval between steps.
This step adds export tests to the existing implementation on
`feat/top-cited-pages`; application behaviour and API contracts are unchanged.
Earlier changes remain in the working tree. No commit, push or deployment.

## Changes and reasons

Added four cases to `frontend/tests/citationSources.test.tsx`:

- Export all returned matches, including a row beyond the 25-row display page,
  using applied rather than unsubmitted draft filters. Check filename, CSV
  contents, UTF-8 BOM, detached download link and object URL cleanup.
- Permit exactly 5,000 returned rows.
- Refuse a partial export when 5,001 match but only 5,000 are returned.
- Disable duplicate requests while pending, display errors, preserve the table
  and allow a successful retry.

Browser download APIs are mocked in jsdom. These tests prove frontend behaviour
at that boundary, not actual browser file persistence or live API availability.

## Validation

Focused Vitest: **10 passed** (6 existing + 4 new). Frontend TypeScript and
touched-file ESLint passed. The initial typecheck found test mock typing issues;
those were fixed before rerunning all three checks successfully. No runtime
defect was found in this step. Existing Vite CJS deprecation warning remains.

## Documentation and operator status

Updated test-suite, implementation-plan and operator-expected with this result.
Scoped documentation review: feature test description and current handoff;
no repository-wide historical-doc audit or deletion. No new architecture or
technical debt; existing feature limitations remain recorded in tech-debt.
Standing resume brief remains intact; no brief was replaced or archived.
No operator action needed for this local test step.

## Next-session prompt

Explain these test additions to the user and wait for their approval before
starting live-data end-to-end validation. Keep the work stepwise and show file
changes with their reasons. Legacy provenance, numeric per-model breakdown and
old summary-card decisions remain unresolved. Preserve all existing unrelated
untracked files. Do not claim live-provider or actual browser-download testing
has been performed in this step.

## Follow-up approved: stored live data and actual browser download

User said "devam" after the test step. Verified the running local stack using
the existing authenticated Chrome session and the real local API, with no
response interception. Used existing Trendyol analysis
`ecb1b2be-cfbd-4935-8870-85c175fed885` (2026-09-17), whose stored provenance is
grounded with explicit dry_run=false. No new provider run was started; this
validates reporting on stored live data, not current external-provider health
or the crawl-to-score lifecycle.

- Pages: 36; domains: 34; eligible answers: 30; questions: 10. Top page:
  four distinct answers, two questions, 13.33% coverage. Evidence dialog showed
  the four answers and corresponding source links; closed successfully.
- Pagination displayed 26–36 on the second page. Switching to Domains reset
  to the first page and showed 34 domains, including a three-page domain union.
- GPT-4o-mini filter: four sources, ten eligible answers. Search `eshipz`:
  one source, 10% coverage. Alternatives group: seven pages, five domains,
  six answers, two questions. Source opportunities: five sources.
- Owned selection and the entered KYC rival domains produced empty results and
  disabled export. This dataset has no owned results and therefore does not
  exercise a positive owned classification in the browser.
- Real Chrome downloads saved two CSV files in the user's Downloads folder:
  `cited-pages-ecb1b2be-cfbd-4935-8870-85c175fed885.csv` (36 data rows) and
  `cited-domains-ecb1b2be-cfbd-4935-8870-85c175fed885.csv` (one filtered row).
  Parsed saved files with Python CSV: BOM present; all 36 coverage values
  independently recomputed correctly; filtered file recorded model, search,
  view, ten-answer denominator and 10% coverage correctly.
- Restored the browser report to its default unfiltered view.

### Findings and next step

All 22 rejected citations belong to GPT-4o-mini: nine answers had structured
citation entries but no bracketed source markers anywhere in the answer. The
current evidence rule correctly excludes those entries. This is a generation
quality limitation; do not silently relax verification. The KYC rival list is
Decathlon, IKEA, UGG, Adidas and Nike, whose suitability for a marketplace-wide
Trendyol comparison needs separate review. No application code was changed.

Next: explain the results and ask user approval before addressing inline-marker
generation or KYC/prompt relevance. A fresh full provider run remains untested.
Documentation refreshed: test-suite, implementation-plan, operator-expected,
tech-debt and this log. No commit/push/deploy or database mutation.

## User correction: industry-first feature, restore existing Citations

User clarified that Top Cited Pages must start from an industry input and run
independently of a company analysis. They requested restoring the original
Citations page and building the industry experience as a separate subpage,
with approval between parts. The marker-repair work was interrupted; its
uncommitted backend changes remain unfinished and must be reviewed before ship.

Restored `frontend/app/ai-visibility/citations/CitationsClient.tsx` exactly to
HEAD (verified zero diff). Preserved the new analysis-bound ranking component
at `frontend/components/ai-visibility/CitationSourcesContent.tsx` for potential
reuse, removed its route wrapper and updated its test import. It is not wired
to a product route. No industry route or job has been implemented yet.

Validation: TypeScript and touched-file ESLint pass; 10 retained component
tests pass; git diff --check clean. These tests cover the preserved component,
not the restored screen. Existing backend/API additions are retained, not
rolled back. Next: approve industry inputs, prompt sampling and data scope
before building the industry subpage. Do not continue competitor/model/type/
trend enhancements against the old analysis-based scope.

## Industry page — part 1, awaiting user review

Added `frontend/app/ai-visibility/industry-citations/page.tsx` and
`IndustryCitationsSetup.tsx`: free-text industry/country, ISO language selector,
review-first/direct workflow choice and local setup preview. Blank/whitespace
inputs are rejected; edits clear the prior preview. No requests, storage or
results are fabricated. State resets on navigation/reload. Existing code change
is limited to the authorized entry in `frontend/lib/shell-nav.ts`, immediately
after Citations, without a Live badge. Original Citations still has zero diff
against HEAD. Existing unfinished backend changes were left untouched.

Validation: typecheck, scoped ESLint, 26 existing navigation/access tests and
diff whitespace check passed. Browser navigation reached ERR_CONNECTION_REFUSED
on localhost:8140; visual and interactive verification is still pending, not
claimed as passed. Country free text is a provisional UI assumption. No new
dependency, migration, provider call, commit, push or deployment.

Updated scope/plan, operator notes and known limitations. Documentation audit
for this slice retained historical implementation notes and explicitly marked
the superseding industry scope; no unrelated documents were removed.

Next-session prompt: wait for the user's part-1 UI review. Then agree on the
question generation/editing contract and ask before changing existing code.
Do not continue the old brand/competitor report or unfinished marker repair.

## 2026-09-19 — industry page part 2, awaiting user review

Added the authenticated `POST /api/v1/industry-citations/questions` contract,
OpenRouter-backed generator, strict ten-question validation, organization-hour
attempt limit and audit records. Added the frontend API client and review/direct
question display. Review mode supports edit/add/remove with duplicate and length
feedback; direct mode is read-only. Analysis execution remains visibly disabled
and no industry run or questions are persisted.

Validation passed: backend Ruff; 16 focused API tests; frontend TypeScript,
scoped ESLint and 5 component tests; OpenAPI and generated TypeScript contracts
were refreshed; diff whitespace check passed. Restarted only the local web/API
containers after their dev reloaders retained old module graphs. Live browser
QA then generated ten editable Turkish questions using `openai/gpt-4o-mini` for
“Koşu ayakkabıları / Türkiye”. This made one paid provider request and one
successful usage/audit entry. No analysis answers or citation rankings ran.

Next: show the endpoint and file-level changes to the user and wait for explicit
part-2 approval. Only then design the persistent industry job, worker dispatch,
search-supported answers and citation evidence collection. Ask before modifying
existing worker/database code.
